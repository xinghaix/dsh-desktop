# Legacy shell (archived)

Primary product path: **Wails + Node Host** (`start:wails` / `start:host` / `package:wails`).

The former NativeWindow / Tray / installer packaging tree is **not** a product fallback.
Sources live under `_deprecated/` for reference only. Legacy Host launcher modes are rejected; use stock Node `host-main`.

See `docs/wails-migration.md` and the 2026-09-06 removal debt evidence note.
