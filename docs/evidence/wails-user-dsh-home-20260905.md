# Wails user dsh home-first (2026-09-05)

Discovery: DSH_BIN > DSH_HOME/chosen > home roots > PATH; packaged only if DSH_ALLOW_PACKAGED_HOST=1.
Missing -> host-install.html. Profile: existing or web.
